# Selección que devuelve el número total de plazas públicas disponibles
sqlite3 parking.db "SELECT COUNT(*) AS plazas_publicas_libres FROM plazas WHERE (tipo = 'publicos' OR tipo = 'electricos' OR tipo = 'minusvalido') AND estado = 'libre';"